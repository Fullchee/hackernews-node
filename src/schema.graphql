type Query {
  info: String!
  randomLink: Link!
}

type Mutation {
  post(url: String, description: String): Media!
  updateLink(id: ID!, url: String, description: String): Link
  deleteLink(id: ID!): Link
}

type Link {
  id: ID!
  description: String!
  url: String
}
